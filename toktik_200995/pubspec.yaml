name: toktik_200995
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  animate_do: ^3.0.2
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  intl: ^0.18.0
  provider: ^6.0.5
  video_player: ^2.5.1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.11.0  # Paquete para cambiar íconos

flutter:
  uses-material-design: true

  assets:
    - assets/videos/

# Configuración para los íconos
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"  # Ruta del ícono en formato PNG
